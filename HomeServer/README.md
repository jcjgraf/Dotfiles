# Homeserver Installation Instruction

- run `sh install.sh -s`
- generate SSL certs and store them in `~/HomeServer/Ssl`
- generate DH an store it in `~/HomeServer/ReverseProxy/dh-param/dhparam-2048.pem`
