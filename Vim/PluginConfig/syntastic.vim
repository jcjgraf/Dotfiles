"let g:syntastic_always_populate_loc_list = 1        " Always and errors to the l-list (might confligt with other plugins)
let g:syntastic_auto_loc_list = 0                   " Automtically open/close l-list
let g:syntastic_check_on_open = 1
let g:syntastic_check_on_wq = 0
let g:syntastic_stl_format = "\ e: %e w: %w\ "
let g:syntastic_ocaml_checkers=['merlin']
